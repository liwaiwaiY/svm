# exp 目录与结果文件清单（2026-08-27 整理）

> 目的：归档 `exp/` 与 `exp/results/` 的文件含义、大小；记录本次清理动作。配套主文档：`2026-08-26-blk-M1M6-macro-app-全实验综合总结.md`。

## 1. exp/ 顶层目录

| 目录 | 大小 | 说明 |
|---|---|---|
| `data/` | 1.4G | 数据集源：`ml-25m/`(1.1G 原始 movielens) + `ml-25.zip/`(250M 压缩包) + `dl.py`/`higgs.py`/`README.md`（数据源脚本） |
| `remote/` | 10G | 数据盘镜像 `fio.raw`（KVM/SVM/NVMe-oF 共用）+ stub 启动脚本 |
| `iscsi/` | 10G | 数据盘镜像 `data_iscsi.raw`（iSCSI 专用）+ 脚本 |
| `local/` | 10G | guest 系统盘 `system.raw` + `svm/dummy.raw`(空洞) + kvm 启动脚本 |
| `results/` | ~13M | 全部实验结果（31 项，见 §3） |
| `scripts/` | ~450K | 全部实验脚本归档（68 个，见 §2） |

## 2. exp/scripts/ 脚本清单（按功能分组）

### 2.1 App 应用脚本（ALS / xgboost / pagerank，最新版 8/26-27）

| 文件 | 大小 | 含义 |
|---|---|---|
| `als_train_bin.py` | 15K | **ALS-BIN 训练主脚本**：CSV 直读 build（build-only 用）+ bin 训练 + 每 iter 强制重扫 + O_DIRECT V checkpoint |
| `als_train.py` / `als_train_v1.py` | 8.9K/6.9K | ALS 旧版（bin 输入 / v1 早期版，保留档案） |
| `gen_als_bin.py` | 1.9K | CSV→紧凑二进制转换（200M 条 12B/条，2.4GB） |
| `gen_synth_xgb.py` | 1.4K | 合成 xgb 数据集生成（schema=HIGGS：1 标签 + 28 特征，seed 固定） |
| `xgb_train.py` | 6.6K | **xgb 训练主脚本**：subprocess 分片 pandas 解析 + 每 round `scan_full` 强制重扫（IO 密集形态） |
| `xgb_parse_seg.py` | 3.6K | xgb 分片解析 worker：零拼接预分配 + 每 chunk fadvise DONTNEED |
| `run_app2.sh` | 11.7K | **App 四协议正式实验编排**（kvm/svm/nvmeof/iscsi × cores 1/2/4，drop_caches + 预热 + journal vacuum + 优雅关机） |
| `run_app_kvm.sh` / `run_app_svm.sh` / `run_app_net.sh` / `run_app_rtt.sh` | 3~11K | App 旧版单协议编排（8/22-25 时代，保留档案） |
| `prep_data.sh` | 3.2K | 数据盘预置脚本（fio.raw/data_iscsi.raw 拷贝 CSV/bin） |
| `io_breakdown.py` / `plot_als_build.py` | 1.7K/2.3K | ALS IO 拆分分析 / build 曲线绘图 |
| `pagerank.py` | 3.0K | Spark PageRank 应用（App 早期实验） |

### 2.2 M 系列 blk 实验（8/24-26）

| 文件 | 含义 |
|---|---|
| `run_m1mem.sh` / `guest_m1mem.sh` | M1 内存重做（iodepth=1，FORCE 重跑）+ guest 侧 |
| `run_m1m5_blk.sh` | M1+M5 大块合并扫描（bs 16K~2M × P on/off） |
| `run_m2g.sh` / `run_m2g_rest.sh` / `run_kvm_m2g.sh` / `host_anchor.sh` | M2G 权威重做（W×I×d 矩阵 + 锚点） |
| `run_m2f.sh` / `run_m2fi.sh` / `m2fi_runner.py` | M2F/M2FI（被 M2G 取代，保留档案） |
| `run_m3_net.sh` / `run_m3_net2.sh` / `gen_m3_net_chart.py` | M3 三协议（SVM/NVMe-oF/iSCSI）RTT 扫描 |
| `run_m3rtt.sh` / `run_m3rtt2.sh` / `run_m3rtt3.sh` | M3RTT 窗口扫描（权威版 rtt3） |
| `run_m4.sh` / `run_m4null.sh` / `run_m4b.sh` / `run_m4c.sh` | M4 batch 系列（batch×iodepth / null_blk / local send batch / 写路径） |
| `run_m5.sh` / `run_m5v.sh` | M5 merge 实验（stub_merge_m knob）+ 验证 |
| `host_switch_blk_null.sh` / `host_switch_kvm_null.sh` | null_blk 切档助手 |
| `parse_m2f.py` / `parse_m2g.py` / `parse_m3_net.py` / `plot_m1mem.py` | 结果解析/绘图 |

### 2.3 Macro（TPCC）与网络存储

`run_macro_kvm.sh`、`run_macro_kvm_anchor.sh`、`run_macro_svm.sh`、`run_macro_svm_fix.sh`（tpcc_run.lua 补丁后）、`run_macro_svm_i256.sh`、`run_macro_b_kvm.sh`、`run_macro_b_svm.sh`（8/25 锚点）、`run_macro_net.sh`（nvmeof/iscsi，含 prep_iscsi_data）、`run_macro_rtt.sh` —— 全部 2.4~12.6K。

### 2.4 Crypto（8/22-25，M5big 被阻断）

`crypto_bench.sh`(1.1K, guest 基准)、`run_crypto_tier.sh`(2.8K)、`run_crypto_m5big.sh`(2.3K)、`host_switch_crypto.sh`(2.5K, 切 tier 0/9)、`aes_bench.c`(6K)、`rsa_sign.c`(3K, 未采用)、`spd_one.c`(1.7K)、`gen_crypto_chart.py`、`parse_crypto.py`。

### 2.5 早期/通用工具

`host_switch.sh`(3.3K, phase a/b 旧机制，phase b 引用已删 data.raw)、`run_tier.sh`(2.6K)、`svm_runner.py`(8.4K)、`run_kvm_ref.sh`(2.4K)、`stall_watch.sh`(2K, 挂起监控)、`backfill.py`(3.2K)、`run_app4.py`(88B, tier4 入口)、`guest_m15.sh`(1.1K)。

## 3. exp/results/ 结果目录（31 项，~13M）

### M 系列（blk）

| 目录 | 大小 | 文件数 | 含义 |
|---|---|---|---|
| `m2g/` | 2.5M | 252 | **M2G 权威重做**（W×I×d 矩阵，json+log） |
| `m2f/` | 2.2M | 174 | M2F（旧，382K 基线作废的源头实验） |
| `m2fi/` | 1.6M | 188 | M2FI（inflight 修复版） |
| `m1m5/` | 568K | 60 | M1+M5 大块合并扫描（P off/on × bs） |
| `m1mem/` | 392K | 40 | M1 内存重做（iodepth=1，含 2 图） |
| `m5/` | 368K | 43 | M5 merge 实验（bs.json 顶层 = M6 数据同目录） |
| `m4new/` | 340K | 40 | M4 batch 重做（batch 1/16/32 × iodepth） |
| `m4local/` | 136K | 15 | M4B local send batch 隔离（null_blk） |
| `m4null/` | 80K | 12 | M4 null_blk |
| `m4c/` | 44K | 5 | M4C 写路径 batch |
| `m3rtt3/` | 192K | 22 | M3RTT3（窗口减半 → 吞吐减半验证） |
| `m3net2/` | 172K | 20 | M3 三协议 RTT 扫描 |
| `network/` | 228K | 27 | 三协议网络对比（含 svg 图 + summary csv） |
| `crypto/` | 100K | 13 | crypto 实验结果（8/22 前） |

### Macro（TPCC）

| 目录 | 大小 | 文件数 | 含义 |
|---|---|---|---|
| `macrosvm/` | 100K | 21 | Macro SVM（数据盘 O_DIRECT 重跑） |
| `macrokvm/` | 64K | 12 | Macro KVM |
| `macro-b/` | 96K | 19 | 8/25 同日锚点（KVM-b 227/315/444、SVM-b 184/197/276） |
| `macronet/` | 96K | 19 | Macro nvmeof/iscsi |
| `macrortt/` | 76K | 18 | Macro RTT 扫描 |

### App

| 目录 | 大小 | 文件数 | 含义 |
|---|---|---|---|
| `app2/` | 524K | 68 | **App 最新版**：als-{kvm,svm,nvmeof,iscsi}、als-bin-csv、xgb-synth(先导)、xgb-synth-6m(IO 密集正式)、xgb-synth-big |
| `app-2/` | 124K | 16 | App 第二轮（als 四协议 + RTT200） |
| `app-net/` | 88K | 13 | App 四协议（spark/wiki 数据） |
| `app-rtt/` | 84K | 12 | App RTT 扫描 |
| `app-rtt200/` | 32K | 6 | App RTT200 |
| `app-rtt300/` | 32K | 6 | App RTT300 |
| `logs/` | 1.1M | 133 | 早期 app 运行日志（8/20-21，保留档案） |

## 4. 本次清理记录（2026-08-27）

**Host 大文件（~18.6G）：**
- `exp/local/kvm/data.raw`（10G）——8/22 时代旧 Phase B 数据盘，现行实验全用 fio.raw/data_iscsi.raw
- `exp/data/ratings_big.csv`（5.7G）——ALS CSV 源（已完结；ml-25m + gen 脚本可重建）
- `exp/data/xgb_synth.csv`（2.75G）——10M 行旧 host 副本（双镜像内已 6M 版）
- `exp/data/xgb_test.csv`（30M）、`SUSY.csv.gz`（172K）
- `/tmp/stub-out.log`（535M）+ 全部 /tmp 运行日志（smoke/app2/macrortt/e2fsck 等）
- 调试文件：`mptest.py`、`xgb_train_noenv.py`、`small.tsv`、`big.tsv`、`exp/nvme/`、`/tmp/als_smoke` 等残渣

**镜像内：** fio.raw + data_iscsi.raw 的 `als_ckpt/`（316M×2）、fio.raw 的 `xgbparse/`（8K）

**scripts 废弃脚本（12 个，无文档引用）：** `run_kvm_now.sh`、`run_kvm_once.sh`、`svm_runner2.py`、`fix_m2f.py`、`fix_m4_d256_r3.py`、`inspect_m4.py`、`inspect_m4b.py`、`verify_m4.py`、`clean_m4.py`、`append_m4_d256_r3.py`、`run_macro_svm_v2.sh`、`guest.log`(0B)

**其他：** `exp/app/`（pagerank.py 与 scripts/ 重复）、`exp/__pycache__/`、`results/app-rtt150b/`（空壳目录）

**数据盘现状：** fio.raw / data_iscsi.raw 仅剩 `xgb_synth.csv`（6M 行 1.66G）；system.raw 保留不动。

## 5. 注意事项

1. **脚本已从 exp/ 顶层归档到 scripts/（8/27 10:20 最终版）**。但 `run_app2.sh` line 128-129 的 scp 源仍是 `$H/exp/$SCRIPT` / `$H/exp/xgb_parse_seg.py`——**下次实验前需把 xgb 相关脚本放回 exp/ 顶层，或改 scp 路径**，否则部署失败。
2. `host_switch.sh` / `run_tier.sh` 的 phase b 引用已删除的 `local/kvm/data.raw`（8/22 时代机制，已过时；保留因 8/22 复现文档引用）。
3. ALS 数据已清空，可随时用 `ml-25m/` + `gen_als_bin.py` 重建；xgb 源数据可用 `gen_synth_xgb.py` 再生成。
4. 结果目录中 `m2f/m2fi`（M2F 旧结论已作废）、`app2/xgb-synth`（先导版）保留作历史对照，未删。
